# Configuration

`fetchext` supports a configuration file at `~/.config/fext/config.toml`.

## Setup

Run `fext setup` to generate a default configuration file.

## Options

```toml
[general]
download_dir = "downloads"
workers = 4
user_agent = "Mozilla/5.0 ..."
```
