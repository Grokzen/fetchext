# Plan: Resumable Downloads (v1.7.0)

## Objective

Implement resumable downloads using HTTP `Range` headers to improve reliability for large files and unstable connections.

## Implementation Steps

1. **Refactor Network Logic**:
    * Add `download_file` function to `src/fetchext/network.py`.
    * Implement `Range` header logic.
    * Handle `206 Partial Content` vs `200 OK`.
    * Integrate `rich` progress bar.

2. **Update Downloaders**:
    * Refactor `ChromeDownloader.download` to use `download_file`.
    * Refactor `EdgeDownloader.download` to use `download_file`.
    * Refactor `FirefoxDownloader.download` to use `download_file`.

3. **Testing**:
    * Add unit tests for `download_file` mocking various server responses (200, 206, 416).
    * Verify integration with existing downloaders.

## Verification

* Run `pytest tests/unit/test_network.py`.
* Run `make test`.
