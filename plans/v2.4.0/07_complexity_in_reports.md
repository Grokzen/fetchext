# Plan: Complexity in Reports

## Goal

Integrate Cyclomatic Complexity metrics into the HTML reports generated by `fext report --html`.

## Steps

1. **Modify `HtmlReporter` in `src/fetchext/reporter.py`**:
    * Extract complexity data from `self.data["complexity"]`.
    * Add a "Complexity Analysis" section to the HTML template.
    * Display summary statistics: Average Complexity, Max Complexity, Total Functions.
    * Add a table listing "High Complexity Functions (>15)".
    * Add a bar chart showing the Top 10 Most Complex Functions.

## Verification

* Run `fext report --html <extension>` on a sample extension.
* Open the generated HTML file and verify the new section and chart exist.
